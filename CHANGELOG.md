# سجل التغييرات (Changelog)

> **قاعدة الترقيم**: MAJOR.MINOR.PATCH — حيث MAJOR للتغييرات الجذرية، MINOR للميزات الجديدة، PATCH للإصلاحات والتحسينات.

## [2.1.0] - 2026-08-30

### الأمان والشبكات المحلية (Security & LAN Hardening)
- 🛡️ **حرج**: تشفير ملف إعدادات الشبكة `lan_config.json` بمفتاح مشتق من عتاد الجهاز لحماية رموز الجلسات على القرص.
- 🛡️ **حرج**: تقييد مسار تصدير قاعدة البيانات `/api/database/export` على الجهاز المضيف محلياً فقط (`127.0.0.1`).
- 🛡️ **حرج**: تحديث شهادات TLS الذاتية التوقيع: تقليل الصلاحية من 10 سنوات إلى سنة واحدة وإصلاح `KeyUsage` لتطابق معايير TLS 1.3.
- 🛡️ **عالي**: دعم التحديث التلقائي لحقول IP SAN في شهادة TLS عند تغيير عنوان IP الجهاز عبر DHCP.
- 🛡️ **عالي**: إضافة طابع زمني `Timestamp` لرسائل UDP Discovery ورفض الحزم التي تجاوز عمرها 30 ثانية لمنع هجمات إعادة البث (Replay Attacks).
- 🛡️ **عالي**: تقييد قائمة الخوادم المكتشفة عبر UDP بـ 50 كحد أقصى مع إزالة التكرار لمنع هجمات فيضان الحزم (UDP Flood DoS).
- 🛡️ **عالي**: حصر عدد أجهزة الكاشير المتصلة بالخادم في نفس الوقت بـ 30 جهازاً ومنع الأجهزة المعلّقة من تجاوز الحظر بإعادة الاتصال.
- 🛡️ **عالي**: فرض فحص انتهاء صلاحية الجلسة (12 ساعة) داخل `RequireAdmin()` في `pkg/auth/session.go`.
- 🛡️ **عالي**: فرض `auth.RequirePermission(PermSettings)` على دوال الأجهزة المتصلة والمحظورة وقطع الاتصال في `lan_handler.go`.
- 🛡️ **عالي**: فرض `auth.RequireAdmin()` على حذف الحسابات السحابية وتعديل الرمز السري `AdminPin`.
- 🛡️ **متوسط**: تقييد قراءة استجابات HTTP في عميل LAN بـ `io.LimitReader` (10 MB) لمنع استهلاك الذاكرة.
- 🛡️ **متوسط**: تخفيف اختناق أقفال التزامن (Lock Contention) في تحديث نشاط الأجهزة وتنظيف تلقائي لسجلات Rate Limiting.
- 🛡️ **متوسط**: توحيد أسماء المستخدمين بالحروف الصغيرة `strings.ToLower` لمنع تضارب الحسابات.
- 🛡️ **منخفض**: إصلاح مسميات صلاحيات موجه الواجهة (`"finance"` و`"reports"` بدلاً من `"view_reports"`).
- 🛡️ **منخفض**: إزالة الترطيب المتفائل غير الآمن للجلسات من `localStorage` في `AuthContext.tsx`.
- 🛡️ **منخفض**: اعتماد مبدأ Fail-Closed عند انتهاء مهلة التهيئة (`useAppInitialization.ts`).

---

## [2.0.9] - 2026-08-30

### الأمان والمعمارية النظيفة (Security & Architecture)
- 🛡️ **حرج**: تطهير كامل لطبقة الخدمات (`service`) من استيراد أو استخدام GORM، ونقل كافة التحديثات الذرية وترجمة الأخطاء (`domain.ErrRecordNotFound`) إلى طبقة الـ `repository` التزاماً ببروتوكول العمارة النظيفة.
- 🛡️ **حرج**: تحصين العمليات الحسابية في الواجهة الأمامية بتطبيق `Math.round` على كافة عمليات ضرب الأسعار بالكميات في 7 ملفات لمنع الانجراف العشري (Floating-Point Drift).
- 🛡️ **حرج**: تشديد حماية الرمز السري الرئيسي `AdminPin` وحصر تعديله بصلاحيات المدير `RequireAdmin()`.
- 🛡️ **عالي**: عزل مسارات صفحات التطبيق بـ `RouteErrorBoundary` لمنع انهيار شاشة نقاط البيع (POS) في حال حدوث استثناء غير متوقع في الصفحات الفرعية.

---

## [2.0.8] - 2026-07-10

### الأمان والثغرات (Security & Vulnerability Fixes)
- 🛡️ **حرج**: إصلاح `UnmarshalJSON` في `money.go` حيث كانت الأعداد الصحيحة تُقرأ كـ cents بدلاً من units
- 🛡️ **حرج**: نقل مفاتيح Google OAuth من `const` في الكود إلى `secureconfig` المشفر
- 🛡️ **عالي**: تمرير Gemini API key عبر HTTP header بدلاً من URL query string
- 🛡️ **عالي**: تشفير GeminiAPIKey في `AppPreferences` في SQLite
- 🛡️ **عالي**: توحيد سلوك `VerifyAdminPin` في جميع المسارات
- 🛡️ **عالي**: تدوير مفاتيح Supabase وتنظيف git history
- 🛡️ **متوسط**: توحيد ثوابت الصلاحيات في `domain/permissions.go` وإزالة التكرار من `pkg/auth/`
- 🛡️ **متوسط**: إصلاح حسابات الأقساط لاستخدام `Amount` بدلاً من `float64`
- 🛡️ **متوسط**: إزالة استخدام `any` من كود الواجهة الأمامية
- 🛡️ **منخفض**: تجميع (precompile) الـ regex في `staff_service.go`

### التحسينات (Improvements)
- 📦 نظام صور المنتجات: ترحيل Base64 من قاعدة البيانات إلى نظام ملفات (Filesystem) مع خادم صور محلي
- 🖼️ تمكين Mica backdrop في Windows 11 للنوافذ الشفافة
- 🔄 إضافة `KeepAliveSupabase` لمنع تعليق مشروع Supabase
- 🌐 دعم الترجمة i18n مع توسيع التغطية النصية
- 🎯 تحسين أداء الشبكة الافتراضية (VirtualProductGrid) مع TanStack Virtual
- 📊 تحسين دقة الحسابات المالية باستخدام نوع `Amount` (int64 cents)

### الإصلاحات (Bug Fixes)
- إصلاح Race Condition في الـ DB transactions أثناء عمليات البيع المتزامنة عبر LAN
- إصلاح فقدان بيانات النافذة (position/size) عند الإغلاق غير الطبيعي
- إصلاح مشكلة تعليق التطبيق عند فشل اتصال Supabase
- إصلاح تسرب الذاكرة في بث Gemini API stream
- إصلاح عدم تحديث أرصدة العملاء بعد الدفع في وضع Offline
- إصلاح عدم تطابق حقول JSON بين Go backend و TypeScript frontend

---

## [2.0.7] - 2026-06-15

### الميزات الجديدة (Features)
- 🤖 إضافة Gemini AI streaming للتوليد التلقائي للتقارير والردود الذكية
- 📊 إضافة لوحة معلومات (Dashboard) مع Sparkline charts ومقارنات شهرية
- 📱 دعم ماسح الباركود USB/HID مع كشف تلقائي و overlay مخصص
- 🔌 إضافة نظام إضافات (Plugin System) مع واجهة `plugin.json` و hooks

### التحسينات (Improvements)
- 🚀 تحسين أداء عرض المنتجات باستخدام Virtual scrolling (TanStack Virtual)
- 🎨 إعادة تصميم شاشة البيع (POS) مع دعم RTL كامل وتحسينات تجربة المستخدم
- 📈 إضافة تحليلات متقدمة للوحة التحكم مع مقارنات شهرية وسنوية

### الإصلاحات (Bug Fixes)
- إصلاح خطأ في حساب الضرائب عند تطبيق خصومات متعددة
- إصلاح مشكلة تعليق التطبيق عند فتح وردية بقيمة كبيرة
- إصلاح مشكلة تزامن LAN عند إعادة الاتصال بعد انقطاع الشبكة

---

## [2.0.6] - 2026-05-20

### الميزات الجديدة (Features)
- 🌐 إضافة LAN Server مع UDP discovery و Heartbeat protocol
- ☁️ إضافة النسخ الاحتياطي السحابي عبر Supabase (تقسيم تلقائي للملفات الكبيرة)
- 💳 إضافة Split Payments (تقسيم الفاتورة على أكثر من طريقة دفع)
- 📋 إضافة Purchase Orders (دورة حياة كاملة لأوامر الشراء)
- 🔐 إضافة تسجيل الدخول بالـ PIN السريع مع خيار `MustChangePin`

### التحسينات (Improvements)
- 🏗️ إعادة هيكلة المشروع إلى Clean Architecture (Handlers → Service → Repository)
- 🔄 استبدال CGO sqlite3 بمكتبة glebarez/sqlite النقية (لا حاجة لـ GCC)
- 📝 تحسين توليد فواتير PDF مع دعم QR code والباركود

### الإصلاحات (Bug Fixes)
- إصلاح خطأ فقدان البيانات عند إغلاق التطبيق أثناء عملية بيع
- إصلاح مشكلة تعدد نسخ التطبيق (Single Instance Lock)
- إصلاح خطأ في حساب نقاط الولاء للعملاء

---

## [2.0.5] - 2026-04-10

### الميزات الجديدة (Features)
- 👥 إضافة نظام الموظفين والصلاحيات (Staff & Roles)
- 📊 إضافة تقارير المبيعات والمخزون
- 💰 إضافة نظام تقسيط العملاء (Installment Plans)
- 🏷️ إضافة نظام الخصومات والعروض (Discounts & Coupons)
- 🔄 إضافة Parked Sales (تعليق الفواتير)

### التحسينات (Improvements)
- 🎨 إعادة تصميم الواجهة بالكامل باستخدام Tailwind CSS + Radix UI
- ⚡ تحسينات أداء كبيرة في عرض قوائم المنتجات
- 🌐 إضافة الترجمة العربية الكاملة (i18n)

---

## [2.0.0] - 2026-03-01

### الإصدار الأساسي (Initial Release)
- 🛒 نقطة بيع أساسية (POS) مع دعم الباركود
- 📦 إدارة المنتجات والمخزون
- 👤 إدارة العملاء
- 💳 إدارة المدفوعات
- 🔧 إعدادات أساسية
- 🖨️ طباعة الفواتير
